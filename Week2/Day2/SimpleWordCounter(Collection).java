/*method wordCounter(articleText) {
    words = split(articleText)
    map<String, Integer> wordCount

    for each word in words:
        clean = normalize(word) // lowercase, remove punctuation
        wordCount[clean]++

    print wordCount
}
*/